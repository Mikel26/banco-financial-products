import { Routes } from '@angular/router';

/**
 * Rutas raíz de la aplicación.
 *
 * El feature `products` (lazy, vía `loadChildren`) y sus rutas hijas
 * (listado, alta, edición) se conectan en la Fase 3.
 */
export const routes: Routes = [];
